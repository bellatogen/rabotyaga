node_modules/
.env
*.bak

# локальные настройки агента и экспорты планов — не коммитим
.claude/settings.local.json
prompt-exports/

# сборка фронтенда — сервер пересобирает при деплое (deploy.sh), в гите не нужна
frontend/dist/
